# ⚡ Быстрое решение проблемы с изображениями

## ✅ Что уже сделано

1. **Исправлен код** в `initialSync.ts` - теперь изображения скачиваются локально
2. **Созданы инструкции** по настройке Supabase Storage
3. **Подготовлен HTML инструмент** для загрузки изображений

## 🚀 Быстрые шаги (5 минут)

### 1️⃣ Настройте Supabase Storage

Откройте [Supabase Dashboard](https://supabase.com/dashboard):

1. **Storage** → **Buckets**
   - Проверьте, есть ли bucket `fathers`
   - Если нет - создайте (New bucket, name: `fathers`, ✅ Public bucket)
   - Если есть - откройте его → Configuration → убедитесь что **Public bucket: ✅**

2. **SQL Editor** - выполните:
   ```sql
   -- Публичное чтение (обязательно!)
   CREATE POLICY IF NOT EXISTS "Public read access"
   ON storage.objects FOR SELECT
   USING (bucket_id = 'fathers');
   ```

### 2️⃣ Загрузите изображения

**Вариант А: Через HTML инструмент (проще всего)**
```bash
# Откройте в браузере
open admin/upload-images.html
```
- Выберите 4 файла из `quotes-of-fathers/assets/` (father-1 до father-4)
- Нажмите Upload
- Скопируйте SQL запросы и выполните в Supabase

**Вариант Б: Через Dashboard**
- Storage → fathers
- Upload 4 файла: `father-1.png`, `father-2.png`, `father-3.png`, `father-4.png`
- Скопируйте их URL-ы
- Обновите таблицу `fathers` с этими URL-ами

### 3️⃣ Проверьте доступность

Откройте в браузере:
```
https://kprqbfxzbclouateifeh.supabase.co/storage/v1/object/public/fathers/father-1.png
```

✅ Видите изображение? Отлично!  
❌ Ошибка 404/403? Вернитесь к шагу 1

### 4️⃣ Протестируйте приложение

```bash
cd quotes-of-fathers

# Удалите приложение с устройства/эмулятора

# Запустите заново
npm start
# или
npx expo start
```

В приложении:
1. Запустите синхронизацию
2. Проверьте логи (должны быть `✅ Avatar downloaded...`)
3. Откройте экран "Все отцы"
4. Изображения должны отображаться! 🎉

## 🆘 Проблемы?

### Изображения не загружаются в Storage

**Причина:** Нет прав на запись  
**Решение:** Добавьте политику:
```sql
CREATE POLICY IF NOT EXISTS "Authenticated insert access"
ON storage.objects FOR INSERT TO authenticated
WITH CHECK (bucket_id = 'fathers');
```

### Изображения не отображаются в приложении

**Причина:** Bucket не публичный  
**Решение:** Storage → fathers → Configuration → Public bucket: ✅

### Ошибка при синхронизации

**Причина:** Неправильные URL в базе  
**Решение:** Проверьте таблицу fathers:
```sql
SELECT id, name_ka, avatar_url FROM fathers WHERE deleted = false;
```
URL должны начинаться с `https://kprqbfxzbclouateifeh.supabase.co/storage/v1/object/public/fathers/`

## 📚 Подробные инструкции

См. файл: `docs/РЕШЕНИЕ_ПРОБЛЕМЫ_С_ИЗОБРАЖЕНИЯМИ.md`

---

**Ожидаемое время:** 5-10 минут  
**Сложность:** Простая  
**Требуется:** Доступ к Supabase Dashboard
