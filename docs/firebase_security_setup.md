# 🔐 Настройка Firebase Security Rules - Пошаговая инструкция

## Обзор

Этот документ описывает, как настроить правила безопасности для Firestore и Firebase Storage для админ-панели проекта "Цитаты Отцов".

---

## ✅ Что уже сделано

1. ✅ Создан файл `firestore.rules`
2. ✅ Создан файл `storage.rules`
3. ✅ Обновлён `firebase.json`
4. ✅ Firestore Rules задеплоены успешно

---

## 🔥 Что нужно сделать

### Шаг 1: Включить Firebase Storage

Firebase Storage ещё не активирован в вашем проекте. Нужно включить его в консоли:

#### Вариант A: Через браузер (рекомендуется)

1. **Откройте ссылку:**
   ```
   https://console.firebase.google.com/project/fathers-b96b5/storage
   ```

2. **Нажмите кнопку "Get Started" (Начать)**

3. **Выберите правила безопасности:**
   - **Production mode** (рекомендуется) - доступ по правилам
   - Или **Test mode** (только для разработки)

4. **Выберите регион хранилища:**
   - Рекомендуется: `europe-west1` (Бельгия) - для Европы/Грузии
   - Или: `us-central1` (США) - дешевле, но медленнее

5. **Нажмите "Done" (Готово)**

#### Вариант B: Через командную строку (автоматически)

Можно попробовать автоматически создать bucket:

```bash
cd /Users/administrator/Desktop/retree/projects/fathers/admin
firebase storage:buckets:create fathers-b96b5.appspot.com --location=europe-west1
```

---

### Шаг 2: Задеплоить Storage Rules

После включения Storage выполните:

```bash
cd /Users/administrator/Desktop/retree/projects/fathers/admin
firebase deploy --only storage:rules
```

**Ожидаемый результат:**
```
✔ storage: released rules storage.rules to fathers-b96b5.appspot.com
✔ Deploy complete!
```

---

### Шаг 3: Проверить что правила активны

#### Проверка Firestore Rules:

1. Откройте: https://console.firebase.google.com/project/fathers-b96b5/firestore/rules
2. Убедитесь, что видите правила из файла `firestore.rules`
3. Должна быть строка: `allow read, write: if request.auth != null;`

#### Проверка Storage Rules:

1. Откройте: https://console.firebase.google.com/project/fathers-b96b5/storage/rules
2. Убедитесь, что видите правила из файла `storage.rules`
3. Должны быть правила для `/fathers/{allPaths=**}`

---

## 📄 Содержимое файлов правил

### firestore.rules

```
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Требовать аутентификацию для всех операций
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
    
    // Более детальные правила для коллекций
    match /fathers/{fatherId} {
      allow read: if request.auth != null;
      allow create, update, delete: if request.auth != null;
    }
    
    match /quotes/{quoteId} {
      allow read: if request.auth != null;
      allow create, update, delete: if request.auth != null;
    }
    
    match /feedback/{feedbackId} {
      allow read: if request.auth != null;
      allow write: if request.auth != null;
    }
    
    match /settings/{settingId} {
      allow read: if request.auth != null;
      allow write: if request.auth != null;
    }
  }
}
```

**Что делают эти правила:**
- ✅ Требуют аутентификацию для всех операций
- ✅ Только залогиненные админы могут читать/писать данные
- ✅ Защищают от анонимного доступа

### storage.rules

```
rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Правила для изображений отцов
    match /fathers/{allPaths=**} {
      // Публичное чтение (для мобильного приложения)
      allow read: if true;
      // Загрузка только для аутентифицированных
      allow write: if request.auth != null;
    }
    
    // Правила для аватаров
    match /avatars/{fileName} {
      allow read: if true;
      allow write: if request.auth != null 
                   && request.resource.size < 10 * 1024 * 1024 // Макс 10 MB
                   && request.resource.contentType.matches('image/.*');
    }
    
    // Правила для профильных изображений
    match /profiles/{fileName} {
      allow read: if true;
      allow write: if request.auth != null 
                   && request.resource.size < 10 * 1024 * 1024 // Макс 10 MB
                   && request.resource.contentType.matches('image/.*');
    }
  }
}
```

**Что делают эти правила:**
- ✅ **Чтение публично** - мобильное приложение может скачивать изображения
- ✅ **Загрузка только для админов** - требуется аутентификация
- ✅ **Ограничение размера** - максимум 10 MB
- ✅ **Только изображения** - проверка MIME-типа

---

## 🧪 Тестирование правил

### Тест 1: Firestore (без аутентификации)

Попробуйте прочитать данные без логина:

```javascript
// Это должно УПАСТЬ с ошибкой permission-denied
const snapshot = await getDocs(collection(db, 'fathers'));
```

**Ожидаемый результат:** ❌ Ошибка `permission-denied`

### Тест 2: Firestore (с аутентификацией)

После логина:

```javascript
// Это должно РАБОТАТЬ
await signInWithEmailAndPassword(auth, email, password);
const snapshot = await getDocs(collection(db, 'fathers'));
```

**Ожидаемый результат:** ✅ Данные получены

### Тест 3: Storage (чтение)

```javascript
// Публичное чтение - должно РАБОТАТЬ без логина
const url = await getDownloadURL(ref(storage, 'fathers/avatar.jpg'));
```

**Ожидаемый результат:** ✅ URL получен

### Тест 4: Storage (загрузка без логина)

```javascript
// Это должно УПАСТЬ
await uploadBytes(ref(storage, 'fathers/test.jpg'), file);
```

**Ожидаемый результат:** ❌ Ошибка `permission-denied`

---

## 🔄 Обновление правил в будущем

Когда нужно изменить правила:

1. **Отредактируйте файлы:**
   - `firestore.rules`
   - `storage.rules`

2. **Задеплойте изменения:**
   ```bash
   # Только Firestore
   firebase deploy --only firestore:rules
   
   # Только Storage
   firebase deploy --only storage:rules
   
   # Всё сразу
   firebase deploy --only firestore:rules,storage:rules
   ```

3. **Проверьте в консоли:**
   - Firestore: https://console.firebase.google.com/project/fathers-b96b5/firestore/rules
   - Storage: https://console.firebase.google.com/project/fathers-b96b5/storage/rules

---

## 🛡️ Безопасность - Best Practices

### ✅ Что СДЕЛАНО правильно:

1. **Требование аутентификации** для записи
2. **Публичное чтение** изображений (для мобильного приложения)
3. **Ограничение размера** файлов (10 MB)
4. **Проверка типа файла** (только изображения)
5. **Раздельные правила** для разных коллекций

### 🔒 Дополнительная безопасность (опционально):

#### 1. Проверка размера документов:

```javascript
// В firestore.rules
match /quotes/{quoteId} {
  allow create: if request.auth != null 
                && request.resource.data.text.ka.size() < 5000; // Макс 5000 символов
}
```

#### 2. Запрет удаления важных данных:

```javascript
match /fathers/{fatherId} {
  allow delete: if false; // Запретить удаление полностью
}
```

#### 3. Валидация полей:

```javascript
match /fathers/{fatherId} {
  allow create: if request.auth != null
                && request.resource.data.name.ka is string
                && request.resource.data.name.ka.size() > 0
                && request.resource.data.avatarUrl is string;
}
```

---

## 📋 Чеклист выполнения пункта 5

- [x] Создан файл `firestore.rules`
- [x] Создан файл `storage.rules`
- [x] Обновлён `firebase.json`
- [x] Firestore Rules задеплоены
- [ ] **Firebase Storage включен в консоли** ⬅️ СДЕЛАЙТЕ ЭТО
- [ ] Storage Rules задеплоены
- [ ] Проверены правила в консоли
- [ ] Протестированы правила (опционально)

---

## 🚀 Следующие шаги

После завершения пункта 5:

**Пункт 6:** Разработка
```bash
npm run dev
```

**Пункт 7:** Деплой
```bash
npm run build
firebase deploy --only hosting
```

---

## 🆘 Troubleshooting

### Проблема: "Storage has not been set up"

**Решение:** Включите Storage в консоли:
https://console.firebase.google.com/project/fathers-b96b5/storage

### Проблема: "permission-denied" при чтении Firestore

**Решение:** 
1. Убедитесь что правила задеплоены: `firebase deploy --only firestore:rules`
2. Проверьте что пользователь залогинен: `auth.currentUser`

### Проблема: "permission-denied" при загрузке в Storage

**Решение:**
1. Убедитесь что правила задеплоены: `firebase deploy --only storage:rules`
2. Проверьте что пользователь залогинен
3. Проверьте размер файла (< 10 MB)
4. Проверьте тип файла (должен быть image/*)

### Проблема: Firestore Rules не применяются

**Решение:**
1. Подождите 1-2 минуты (правила применяются с задержкой)
2. Очистите кэш браузера
3. Перезагрузите приложение

---

## ✅ Итог

После выполнения всех шагов у вас будет:

- 🔒 **Защищённая база данных** - только админы могут читать/писать
- 🔒 **Защищённое хранилище** - только админы могут загружать файлы
- 🌐 **Публичные изображения** - мобильное приложение может их скачивать
- ✅ **Правильная конфигурация** - готово к production

**Время выполнения:** 5-10 минут

---

**Готово к следующему шагу! 🎉**
