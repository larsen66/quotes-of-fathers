# ПРОЕКТ: «Цитаты Отцов»

## ИНСТРУКЦИЯ ДЛЯ ИИ-АССИСТЕНТОВ

**КРИТИЧЕСКИ ВАЖНО:** При работе над этим проектом ИИ-ассистент ОБЯЗАН строго следовать всем требованиям из документации. Все требования с идентификаторами являются обязательными.

---

## 📚 ОБЯЗАТЕЛЬНЫЕ ДОКУМЕНТЫ ДЛЯ ЧТЕНИЯ

Перед началом любой работы ИИ-ассистент ДОЛЖЕН прочитать следующие документы:

1. **`docs/func_req.md`** — Технические требования (TR-*)
   - Технологический стек
   - Офлайн-режим
   - Синхронизация
   - Мультиязычность
   - Definition of Done

2. **`docs/admin_panel_func_req.md`** — Функциональные требования админ-панели (FR-A*)
   - Структура админ-панели
   - Управление контентом
   - Мультиязычность интерфейса

3. **`docs/dm_req.md`** — Модель данных (DM-*)
   - Структура сущностей
   - Supabase и SQLite схемы
   - Правила локализации

4. **`docs/supabase_setup.md`** — Настройка Supabase
   - Инструкции по развёртыванию
   - Конфигурация Storage
   - Row Level Security

---

## 🤖 ПРАВИЛА РАБОТЫ ДЛЯ ИИ

### Правило 1: Обязательное чтение документов
**ПЕРЕД** началом любой задачи:
- ✅ Прочитать соответствующие разделы всех документов
- ✅ Найти все требования с идентификаторами, относящиеся к задаче
- ✅ Убедиться, что понятны все связанные требования

### Правило 2: Строгое соответствие требованиям
- **ВСЕ** требования с идентификаторами (TR-*, FR-A*, DM-*) являются обязательными
- **НЕ ДОБАВЛЯТЬ** функциональность, не описанную в документах
- **НЕ ИЗМЕНЯТЬ** требования без явного разрешения пользователя
- При генерации кода **ССЫЛАТЬСЯ** на конкретные требования (например: "реализует TR-3.1, TR-LANG-1.1")

### Правило 3: Технологический стек (строго фиксирован)
Использовать **ТОЛЬКО** указанные технологии:

```
Бэкенд:
- Supabase (PostgreSQL, Storage, Auth)

Админ-панель:
- React (с TypeScript)
- Supabase JS Client для backend операций
- Vercel/Netlify для хостинга
- Supabase Auth для аутентификации

Мобильное приложение:
- React Native + Expo (Managed Workflow)
- SQLite (expo-sqlite) для локального хранилища
- expo-file-system для файлов
- Supabase JS Client для синхронизации
```

**ЗАПРЕЩЕНО** предлагать альтернативные технологии без явного запроса пользователя.

### Правило 4: Модель данных (точное соответствие)
- Все сущности, поля и типы должны **ТОЧНО** соответствовать `dm_req.md`
- Серверная модель (Supabase) — раздел 2.1, 3.1 в `dm_req.md`
- Локальная модель (SQLite) — раздел 2.2, 3.2 в `dm_req.md`
- **НЕ ДОБАВЛЯТЬ** поля, не описанные в документации

### Правило 5: Мультиязычность (обязательна)
**КРИТИЧЕСКИ ВАЖНО:**
- Поддерживаются **ТОЛЬКО** два языка: `ka` (грузинский) и `ru` (русский)
- Грузинский (`ka`) — **обязательный**, основной язык
- Русский (`ru`) — опциональный, fallback на `ka` при отсутствии
- Все текстовые поля контента: `name_ka`, `name_ru`, `text_ka`, `text_ru`, `bio_ka`, `bio_ru`, `source_ka`, `source_ru`
- Язык интерфейса хранится в `Settings.language` ("ka" | "ru")
- Переключение языка работает **офлайн**

### Правило 6: Офлайн-режим (обязателен)
- Приложение **ДОЛЖНО** работать полностью офлайн после первой загрузки
- Все данные отображаются **ТОЛЬКО** из локальной базы SQLite
- Supabase используется **ТОЛЬКО** для синхронизации
- Синхронизация выполняется в фоне, не блокирует UI
- Все переводы должны быть доступны офлайн

### Правило 7: Валидация обязательных полей
Обязательные поля (должны валидироваться):
- `Father.name_ka` (TR-LANG-3.1, FR-A9-1.1)
- `Father.avatar_url` (FR-A9-1.1)
- `Quote.father_id` (FR-A9-1.1)
- `Quote.text_ka` (TR-LANG-4.1, FR-A9-1.1)

---

## 🔍 ЧЕКЛИСТ ДЛЯ ИИ ПЕРЕД ГЕНЕРАЦИЕЙ КОДА

Перед генерацией любого кода ИИ должен проверить:

- [ ] Прочитаны все релевантные разделы документации
- [ ] Найдены все требования с идентификаторами для задачи
- [ ] Используется правильный технологический стек
- [ ] Модель данных соответствует `dm_req.md`
- [ ] Реализована мультиязычность (если применимо)
- [ ] Учтён офлайн-режим (если применимо)
- [ ] Валидация обязательных полей (если применимо)
- [ ] Код соответствует всем найденным требованиям

---

## 📝 ФОРМАТ ССЫЛОК НА ТРЕБОВАНИЯ

При генерации кода или объяснении решения ИИ должен ссылаться на требования:

**Правильный формат:**
```
// Реализует TR-3.1: React Native + Expo (Managed Workflow)
// Соответствует DM-LANG-1: поддержка языков ka и ru
// Выполняет FR-A3-2.1: форма создания отца с полями name_ka, name_ru
```

**Неправильный формат:**
```
// Использует React Native (без ссылки на требование)
```

---

## ⚠️ ЧТО ЗАПРЕЩЕНО ДЛЯ ИИ

1. **ЗАПРЕЩЕНО** предлагать альтернативные технологии без запроса
2. **ЗАПРЕЩЕНО** добавлять поля/функции, не описанные в документах
3. **ЗАПРЕЩЕНО** изменять структуру данных без согласования
4. **ЗАПРЕЩЕНО** игнорировать требования с идентификаторами
5. **ЗАПРЕЩЕНО** предлагать решения, противоречащие документации
6. **ЗАПРЕЩЕНО** использовать технологии, не указанные в стеке

---

## ✅ ПРИОРИТЕТЫ ПРИ РАБОТЕ

1. **ВЫСШИЙ ПРИОРИТЕТ:** Соответствие требованиям с идентификаторами (TR-*, FR-A*, DM-*)
2. **ВЫСШИЙ ПРИОРИТЕТ:** Офлайн-режим и мультиязычность
3. **ВЫСОКИЙ ПРИОРИТЕТ:** Точное соответствие модели данных
4. **ВЫСОКИЙ ПРИОРИТЕТ:** Использование правильного технологического стека

---

## 🎯 ПРИМЕРЫ ПРАВИЛЬНОЙ РАБОТЫ ИИ

### Пример 1: Создание модели Father
```typescript
// ✅ ПРАВИЛЬНО: соответствует dm_req.md раздел 2.2
interface Father {
  id: string; // PRIMARY KEY
  name_ka: string; // обязательное (DM-LANG-1, FR-A3-2.1)
  name_ru?: string; // опционально (DM-LANG-1, FR-A3-2.1)
  bio_ka?: string; // опционально (FR-A3-2.1)
  bio_ru?: string; // опционально (FR-A3-2.1)
  avatarLocalPath: string; // TR-IMG-1.3
  profileLocalPath?: string;
  order?: number;
  updatedAt: string;
}
```

### Пример 2: Функция локализации
```typescript
// ✅ ПРАВИЛЬНО: соответствует DM-UI-1
function getLocalizedText(field: { ka: string; ru?: string }, language: "ka" | "ru"): string {
  return field[language] ?? field["ka"]; // fallback на ka (DM-LANG-2)
}
```

### Пример 3: SQLite схема
```sql
-- ✅ ПРАВИЛЬНО: соответствует dm_req.md раздел 2.2, TR-LS-1
CREATE TABLE fathers (
  id TEXT PRIMARY KEY,
  name_ka TEXT NOT NULL, -- обязательное (FR-A9-1.1)
  name_ru TEXT,
  bio_ka TEXT,
  bio_ru TEXT,
  avatarLocalPath TEXT NOT NULL, -- TR-IMG-1.3
  profileLocalPath TEXT,
  "order" INTEGER,
  updatedAt TEXT NOT NULL
);
```

---

## 📋 СПИСОК ВСЕХ ИДЕНТИФИКАТОРОВ ТРЕБОВАНИЙ

### Технические требования (func_req.md)
- **TR-1.** Технологический стек
- **TR-2.** Админ-панель
- **TR-3.** Мобильное приложение
- **TR-OFF-*** Офлайн-режим
- **TR-LS-*** Локальное хранение
- **TR-IMG-*** Изображения
- **TR-SYNC-*** Синхронизация
- **TR-DEL-*** Удаление данных
- **TR-APP-*** Поведение приложения
- **TR-G-*** Гарантии и ограничения
- **TR-LANG-*** Мультиязычность

### Функциональные требования админ-панели (admin_panel_func_req.md)
- **FR-A0-*** Общие положения
- **FR-A1-*** Структура админ-панели
- **FR-A2-*** Дашборд
- **FR-A3-*** Раздел "Отцы"
- **FR-A4-*** Раздел "Цитаты"
- **FR-A5-*** Подписчики
- **FR-A6-*** Обратная связь
- **FR-A7-*** Медиа
- **FR-A8-*** Синхронизация
- **FR-A9-*** Валидация
- **FR-A10-*** Мультиязычность

### Модель данных (dm_req.md)
- **DM-LANG-*** Принцип локализации
- **DM-UI-*** Правила отображения текста

---

## 🔄 ПРОЦЕСС РАБОТЫ ИИ НАД ЗАДАЧЕЙ

1. **Получение задачи от пользователя**
2. **Чтение релевантных разделов документации**
3. **Поиск всех связанных требований с идентификаторами**
4. **Проверка соответствия технологическому стеку**
5. **Генерация кода с соблюдением всех требований**
6. **Добавление комментариев со ссылками на требования**
7. **Проверка по чеклисту перед отправкой**

---

## ⚠️ КРИТИЧЕСКИЕ ТРЕБОВАНИЯ (НЕ МОГУТ БЫТЬ ИГНОРИРОВАНЫ)

1. **TR-OFF-1.1:** Полная работа офлайн после первой загрузки
2. **TR-LS-2.1:** Данные всегда из SQLite для отображения
3. **DM-LANG-1:** Поддержка языков ka и ru
4. **DM-UI-1:** Fallback на ka при отсутствии перевода
5. **FR-A9-1.1:** Валидация обязательных полей
6. **TR-3.1:** React Native + Expo (Managed Workflow)
7. **TR-1.2:** Supabase для хранения данных

---

## 📞 ДЕЙСТВИЯ ПРИ НЕЯСНОСТЯХ

Если ИИ встречает неясность или противоречие:

1. **Сначала:** Перечитать соответствующие разделы всех документов
2. **Затем:** Указать пользователю на конкретные разделы документации
3. **Спросить:** Уточнить у пользователя, если вопрос остаётся неясным
4. **НЕ ПРЕДПРИНИМАТЬ:** Самостоятельных решений, противоречащих документации

---

**ПОМНИТЕ:** Все требования с идентификаторами являются обязательными. Разработка ведётся СТРОГО по документам.
