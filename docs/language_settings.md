# Настройки языка / Language Settings

## Обзор

В обоих приложениях (мобильное и админ-панель) реализована возможность смены языка интерфейса через настройки.

## Мобильное приложение (React Native)

### Расположение
Настройки → Язык приложения (в верхней части экрана)

### Доступные языки
- 🇬🇪 ქართული (Грузинский)
- 🇷🇺 Русский

### Как работает
1. Пользователь выбирает язык в настройках
2. Нажимает кнопку "Сохранить"
3. Язык применяется мгновенно
4. Настройка сохраняется в локальной базе данных SQLite

### Файлы
- **UI**: `/quotes-of-fathers/src/app/screens/SettingsScreen.tsx`
- **Локализация**: `/quotes-of-fathers/src/i18n/i18n.ts`
- **Переводы**: 
  - `/quotes-of-fathers/src/i18n/locales/ka.json`
  - `/quotes-of-fathers/src/i18n/locales/ru.json`
- **Хранение**: `/quotes-of-fathers/src/data/db/repositories/settingsRepo.ts`

### Особенности
- Язык по умолчанию определяется из настроек устройства
- Если язык устройства не поддерживается, используется грузинский
- Настройка сохраняется между запусками приложения

## Админ-панель (React Web)

### Расположение
Настройки → Язык (первая карточка на странице)

### Доступные языки
- 🇬🇪 ქართული (Грузинский)
- 🇷🇺 Русский

### Как работает
1. Пользователь выбирает язык с помощью переключателя
2. Язык применяется мгновенно без необходимости сохранения
3. Настройка сохраняется в localStorage браузера

### Файлы
- **UI**: `/admin/src/pages/SettingsPage.tsx`
- **Контекст**: `/admin/src/contexts/LocaleContext.tsx`
- **Локализация**: встроена в `LocaleContext.tsx` (i18next)

### Особенности
- Язык по умолчанию: русский
- Настройка сохраняется в localStorage под ключом `admin-locale`
- Переключение происходит мгновенно без перезагрузки страницы

## Дизайн интерфейса

### Мобильное приложение
- Современный сегментированный контрол (iOS-стиль)
- Активная кнопка выделяется белым фоном с тенью
- Расположена в верхней части экрана настроек для удобства доступа
- Секция "Язык приложения" с заголовком

### Админ-панель
- Material-UI ToggleButtonGroup
- Флаги стран для визуальной идентификации
- Карточка с описанием функции
- Расположена первой на странице настроек

## Технические детали

### Мобильное приложение
```typescript
// Структура настроек в SQLite
interface Settings {
  language: 'ka' | 'ru';
  notificationsEnabled: number;
  weekdayTime: string;
  weekendTime: string;
  soundId: string;
}

// Применение языка
await i18n.changeLanguage(language);
```

### Админ-панель
```typescript
// Контекст локализации
interface LocaleContextType {
  locale: 'ka' | 'ru';
  setLocale: (locale: Locale) => void;
  t: (key: string) => string;
}

// Сохранение в localStorage
localStorage.setItem('admin-locale', locale);
```

## Добавление нового языка

### Мобильное приложение
1. Создать файл `/quotes-of-fathers/src/i18n/locales/{код_языка}.json`
2. Добавить импорт в `/quotes-of-fathers/src/i18n/i18n.ts`
3. Обновить `SUPPORTED_LANGS` в `i18n.ts`
4. Добавить кнопку в `SettingsScreen.tsx`
5. Обновить тип `AppLanguage`

### Админ-панель
1. Добавить переводы в `resources` в `LocaleContext.tsx`
2. Обновить тип `Locale`
3. Добавить кнопку в `SettingsPage.tsx`

## Рекомендации

1. **Консистентность**: Убедитесь, что все новые ключи переводов добавлены во все языковые файлы
2. **Тестирование**: Проверьте все экраны после смены языка
3. **Fallback**: Всегда указывайте fallback язык (по умолчанию грузинский)
4. **Форматирование**: Используйте интерполяцию i18next для динамических значений

## Известные ограничения

1. В мобильном приложении требуется нажатие кнопки "Сохранить" для применения языка
2. В админ-панели язык не синхронизируется между вкладками браузера автоматически
3. Язык админ-панели не влияет на язык мобильного приложения (это разные настройки)
